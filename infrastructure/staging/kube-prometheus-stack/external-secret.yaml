apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-admin-creds
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: aws-secrets-store       # Your ClusterSecretStore
    kind: ClusterSecretStore
  target:
    name: grafana-admin-creds
    creationPolicy: Owner
  data:
    - secretKey: admin-user
      remoteRef:
        key: grafana-admin-creds   # AWS Secret Name
        property: admin-user       # JSON property
    - secretKey: admin-password
      remoteRef:
        key: grafana-admin-creds
        property: admin-password

